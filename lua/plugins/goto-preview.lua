-- 跳转到定义预览框
return {
  {
    "rmagatti/goto-preview",
    -- keys = {
    --   { "gpd", "<cmd>lua require('goto-preview').goto_preview_definition()<CR>", desc = "预览定义" },
    -- },
    opts = {
      default_mappings = true,
    },
  },
}
